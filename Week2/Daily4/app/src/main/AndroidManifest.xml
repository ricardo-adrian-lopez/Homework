<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobileapps.training.week2daily4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BaseActivity"
            android:label="BaseActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PictureActivity"
            android:label="@string/nav_camera"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CallNumber"
            android:label="Call a number"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ShareString"
            android:label="Share String"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>